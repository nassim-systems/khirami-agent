# Widget

Guide d'intégration du widget embeddable `widget-v2.js`.

- Installation: inclure le script et initialiser avec `clientId` et `authToken`.
- Comportement: envoie payload complet et gère l'état UI.
